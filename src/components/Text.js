import styled from 'styled-components/native';
import { FONT_STANDARD, TEXT_COLOR } from '../config/const';

const Text = styled.Text`
  font-family: ${FONT_STANDARD};
  color: ${TEXT_COLOR};
`;

export default Text;
